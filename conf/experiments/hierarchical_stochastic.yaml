# @package _global_

defaults:
  - /train
  - override /dataset: classification/hierarchical_stochastic
  - _self_

student:
  dim: 50                       # = dataset.chunk_dim — observed vocabulary
  pe_max_sequence_length: 64    # accommodate observed length = (prefix+length)*chunk_size = 52
  attention_bias: false

misc:
  wandb:
    tags: ["hierarchical-stochastic"]
